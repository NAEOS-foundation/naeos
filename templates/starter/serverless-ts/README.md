# serverless-ts

TypeScript serverless project with AWS Lambda, API Gateway, and DynamoDB.

## Structure

- `src/api/` — API Gateway handlers
- `src/auth/` — Authentication handlers

## Getting Started

```bash
naeos init
naeos run
```
