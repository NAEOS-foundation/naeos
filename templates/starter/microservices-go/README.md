# microservices-go

Go microservices starter project with gRPC, REST API, and event-driven communication.

## Structure

- `api-gateway/` — REST/HTTP gateway
- `services/user/` — User gRPC service
- `services/order/` — Order gRPC service

## Getting Started

```bash
naeos init
naeos run
```
