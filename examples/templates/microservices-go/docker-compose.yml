services:
  api-gateway:
    build: .
    command: ["go", "run", "./cmd/api"]
    environment:
      PORT: "8080"
    ports:
      - "8080:8080"
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
