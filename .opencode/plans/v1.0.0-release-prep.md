# v1.0.0 Release Prep Plan

## Current Status (v0.9.0)
- Build: clean
- go vet: clean
- Tests: 104/104 packages pass, 0 fail
- Coverage: 61.6% overall
- Go files: 271 source + 136 test
- TODO/FIXME: 0 (clean)

## Task 1: Fix CodeQL Go Version Mismatch
**File:** `.github/workflows/codeql.yml` line 28
**Change:** `go-version: "1.22"` → `go-version: "1.25"`

## Task 2: Add tests for internal/pluginsdk
**File:** `internal/pluginsdk/sdk.go` — Deprecated wrapper, only type aliases and var redirects
**File to create:** `internal/pluginsdk/sdk_test.go`
**Tests needed:**
- Verify type aliases resolve correctly (Plugin, PluginContext, Logger, etc.)
- Verify const values match pluginhost (StateCreated, StateInitialized, etc.)
- Verify var functions are callable (NewManager, NewSimpleLogger, etc.)
- Test that NewManager returns a valid Manager instance

## Task 3: Improve internal/database coverage (29.5% → 70%+)
**Source:** `internal/database/database.go` (582 lines) — 3 adapters (PostgreSQL, MySQL, SQLite), Manager, Pool, Transactions
**Existing tests:** `internal/database/database_test.go` (267 lines) — basic happy paths
**Uncovered code:**
- MySQL: Query, QueryRow, Begin, Migrate, Rollback, Transaction (Exec, Query, Commit, Rollback)
- SQLite: Query, QueryRow, Begin, Migrate, Rollback, Transaction (Exec, Query, Commit, Rollback)
- PostgreSQL Rollback: version-based rollback logic
- Pool: Get from empty pool, Put to full pool (triggers Close)
- Manager: ConnectAll with missing config, List when empty
- RealPostgreSQL: covered by build tag `!nosql`, skip for unit tests

**New test cases for `database_test.go`:**
1. `TestMySQLFullLifecycle` — Connect, Ping, Exec, Query, QueryRow, Begin, Migrate, Rollback, Close
2. `TestMySQLNotConnected` — all methods return error when not connected
3. `TestMySQLTransaction` — Begin, Exec, Query, Commit
4. `TestMySQLTransactionRollback` — Begin, Exec, Rollback
5. `TestSQLiteFullLifecycle` — same as MySQL
6. `TestSQLiteNotConnected` — same as MySQL
7. `TestSQLiteTransaction` — same as MySQL
8. `TestSQLiteTransactionRollback` — same as MySQL
9. `TestPostgreSQLRollback` — Migrate then Rollback to lower version
10. `TestPoolEmptyGet` — Get from empty pool returns nil
11. `TestPoolOverflow` — Put more than maxOpen triggers Close on overflow connection
12. `TestManagerGetNotFound` — Get non-existent name returns false
13. `TestManagerListEmpty` — List on empty manager returns empty slice

## Task 4: Improve internal/websocket coverage (31.6% → 70%+)
**Source:** `internal/websocket/server.go` (243 lines) + `observer.go` (33 lines)
**Existing tests:** `server_test.go` (75 lines) + `observer_test.go` (18 lines)
**Uncovered code:**
- Server.Run(): register/unregister/broadcast channels
- Server.Stop(): graceful close with timeout
- Server.ClientCount(): after client connect
- Client.readPump(): message processing, ping/pong
- Client.writePump(): message sending, ping ticker
- HandleWebSocket: upgrade failure path
- Broadcast: full client list
- EventBroadcaster methods

**New test cases for `server_test.go`:**
1. `TestServerRegisterUnregister` — start Run goroutine, send register, verify ClientCount, send unregister
2. `TestServerBroadcastToClients` — register client, broadcast message
3. `TestServerBroadcastFullChannel` — client with full send channel
4. `TestStopWithNoClients` — Stop on empty server
5. `TestGenerateIDUniqueness` — generate 100 IDs, all unique
6. `TestEventBroadcasterAllMethods` — test all 6 EventBroadcaster methods
7. `TestWSObserverAllMethods` — test all 4 WSObserver methods

**Note:** Full WebSocket connection tests (HandleWebSocket, readPump, writePump) require httptest + gorilla/websocket client — complex but valuable.

## Task 5: Improve internal/migration coverage (33.1% → 70%+)
**Source:** `internal/migration/engine.go` (206 lines) + `migration.go` (140 lines)
**Existing tests:** `migration_test.go` (71 lines)
**Uncovered code:**
- MigrationEngine: Migrate, Plan, AvailableVersions, VersionBetween, FormatMigrationPlan
- MigrationPlanner: AddStep, Plan with custom steps
- MigrationPlanner.Plan: invalid version, same version
- MigrationPlanner.Migrate: multi-step migration
- Builtin transforms: 0.1.0→0.2.0 (module path, dependencies, generation), 0.2.0→0.3.0 (architecture, security, testing)
- Version.Less: same version, patch comparison
- VersionBetween: invalid versions, edge cases

**New test cases for `migration_test.go`:**
1. `TestMigrationEngineMigrate010To030` — full migration with data
2. `TestMigrationEngineMigrateNoPath` — from version with no path (e.g., "9.0.0" to "0.3.0")
3. `TestMigrationEnginePlan` — Plan returns correct steps
4. `TestMigrationEngineAvailableVersions` — returns ["0.1.0", "0.2.0", "0.3.0"]
5. `TestMigrationEngineVersionBetween` — various combinations
6. `TestFormatMigrationPlanEmpty` — "No migrations needed."
7. `TestFormatMigrationPlanMultiple` — formatted output
8. `TestPlannerAddStep` — custom migration step
9. `TestPlannerPlanInvalidVersion` — parse error
10. `TestPlannerPlanSameVersion` — error
11. `TestPlannerMigrateMultiStep` — 0.1.0 → 0.3.0 with step execution
12. `TestBuiltinTransform010To020` — verify module path, dependencies, generation added
13. `TestBuiltinTransform020To030` — verify architecture, security, testing added
14. `TestVersionLessEqual` — same version returns false
15. `TestVersionLessPatch` — patch-level comparison

## Task 6: Improve internal/marketplace coverage (34.3% → 70%+)
**Source:** `internal/marketplace/marketplace.go` (167 lines) + `remote.go` + `signature.go`
**Existing tests:** `marketplace_test.go` (84 lines) + `remote_test.go` + `signature_test.go`
**Uncovered code:**
- RegistryClient.Install: file write
- RegistryClient.Publish: update existing entry
- RegistryClient.loadCache: corrupted JSON, read error
- contains/containsSubstr: edge cases (empty string, exact match, longer substring)
- SearchFilter.Limit: exact boundary
- defaultEntries: version constants

**New test cases for `marketplace_test.go`:**
1. `TestInstall` — Install a default entry, verify file content
2. `TestInstallNotFound` — Install non-existent entry
3. `TestPublishUpdate` — Publish entry with same name updates it
4. `TestSearchLimit` — Search with limit=1 returns only 1 result
5. `TestSearchNoMatch` — Search with non-matching query returns empty
6. `TestSearchMultipleTags` — Search with multiple tags, entry must match at least one
7. `TestContainsExactMatch` — contains("abc", "abc") = true
8. `TestContainsSubstrMatch` — contains("abcde", "bcd") = true
9. `TestContainsNoMatch` — contains("abc", "xyz") = false
10. `TestContainsEmptyQuery` — contains("abc", "") = true
11. `TestLoadCacheCorrupted` — write invalid JSON to registry.json, verify error
12. `TestDefaultEntriesVersion` — verify version constants are set

## Task 7: Improve internal/api coverage (35.3% → 70%+)
**Source:** `internal/api/server.go` (1197 lines) + middleware.go + jwt.go + requestid.go
**Existing tests:** `server_test.go` (224 lines) + middleware_test.go + jwt_test.go + requestid_test.go
**Uncovered code:**
- handlePipelineStatus, handleArtifacts (DELETE), handleContextGenerate
- handleMCPMessage (validate_spec, compile_spec, generate_context, explain_concept, list_artifacts, get_pipeline_status, export_terraform, list_plugins)
- handleCloudPlan, handleCloudDeploy, handleCloudDestroy, handleCloudStatus
- handlePlugins, handlePluginExecute, handlePluginByName
- handleVersion, handleConfigSchema, handlePipelines
- handleMetrics, handleHealthz, handleReadyz
- handlerWithMiddleware: CORS, rate limiting, body size limits, RBAC
- RegisterAPIKey

**New test cases for `server_test.go`:**
1. `TestPipelineStatusEndpoint` — GET /api/v1/pipeline/status
2. `TestArtifactsEndpointDELETE` — DELETE /api/v1/artifacts
3. `TestContextGenerateEndpoint` — POST /api/v1/context/generate
4. `TestMCPMessageEndpoint` — POST /api/v1/mcp/message
5. `TestCloudPlanEndpoint` — POST /api/v1/cloud/plan
6. `TestCloudDeployEndpoint` — POST /api/v1/cloud/deploy
7. `TestCloudDestroyEndpoint` — POST /api/v1/cloud/destroy
8. `TestCloudStatusEndpoint` — GET /api/v1/cloud/status
9. `TestPluginsEndpointGET` — GET /api/v1/plugins
10. `TestPluginExecuteEndpoint` — POST /api/v1/plugins/execute
11. `TestPluginByNameEndpoint` — DELETE /api/v1/plugins/{name}
12. `TestVersionEndpoint` — GET /api/v1/version
13. `TestConfigSchemaEndpoint` — GET /api/v1/config/schema
14. `TestPipelinesEndpoint` — GET /api/v1/pipelines
15. `TestMetricsEndpoint` — GET /metrics
16. `TestHealthzEndpoint` — GET /healthz
17. `TestReadyzEndpoint` — GET /readyz
18. `TestHandlerWithMiddleware` — test CORS headers, rate limiting
19. `TestRegisterAPIKey` — register and verify API key rate limiter

## Task 8: Improve cmd/naeos coverage (37.0% → 50%+)
**Source:** `cmd/naeos/` (68 files)
**Strategy:** Add tests for simpler commands that don't require full pipeline execution.
**New test cases:**
1. `TestVersionCommand` — execute version command, verify output
2. `TestDoctorCommand` — execute doctor command
3. `TestHealthCommand` — execute health command
4. `TestConfigShowCommand` — execute config show command
5. `TestConfigValidateCommand` — execute config validate command
6. `TestStatusCommand` — execute status command

## Task 9: Improve internal/pluginsdk/sandbox coverage (41.5% → 70%+)
**Source:** `internal/pluginsdk/sandbox/` — JSON-over-stdin/stdout protocol + WASM execution
**Strategy:** Add tests for sandbox creation, timeout, error handling.

## Task 10: Improve internal/testrunner coverage (41.6% → 70%+)
**Source:** `internal/testrunner/runner.go` (231 lines)
**Existing tests:** `runner_test.go` (138 lines)
**Uncovered code:**
- runGoTests: actual execution path
- runNodeTests: package.json check, pnpm detection
- runPythonTests: pytest execution
- runJavaTests: pom.xml, build.gradle detection
- runRustTests: cargo test execution
- detectLanguages: all marker file checks
- RunAll with specific languages

**New test cases:**
1. `TestDetectLanguagesTypescript` — create temp package.json, verify detection
2. `TestDetectLanguagesPython` — create temp requirements.txt
3. `TestDetectLanguagesJava` — create temp pom.xml
4. `TestDetectLanguagesRust` — create temp Cargo.toml
5. `TestDetectLanguagesNone` — empty dir detects nothing
6. `TestRunLanguageNodeNotFound` — no package.json
7. `TestRunLanguageJavaNotFound` — no pom.xml or build.gradle
8. `TestRunAllWithErrors` — RunAll with failing language

## Task 11: Improve internal/watch coverage (41.7% → 70%+)
**Source:** `internal/watch/watch.go` (244 lines)
**Existing tests:** `watch_test.go` (125 lines)
**Uncovered code:**
- PipelineWatcher: Start, Stop, shouldProcess, runPipeline
- Watcher.Run: fsnotify event loop, debounce
- Watcher.DetectChanges: modified events

**New test cases:**
1. `TestPipelineWatcherShouldProcess` — test .yaml, .yml, .json → true; .txt → false
2. `TestPipelineWatcherNew` — verify defaults
3. `TestDetectChangesModified` — modify existing file, detect "modified" event
4. `TestDetectChangesEmpty` — no changes returns empty

## Task 12: Improve internal/configschema coverage (46.9% → 70%+)
**Source:** `internal/configschema/schema.go` (146 lines)
**Existing tests:** `schema_test.go` (92 lines)
**Uncovered code:**
- ValidateFile: YAML path, JSON path, unknown extension
- validateYAML: error path
- validateJSON: error path
- validateType: number type, object type, unknown type (default true)

**New test cases:**
1. `TestValidateFileYAML` — write temp YAML config, validate
2. `TestValidateFileJSON` — write temp JSON config, validate
3. `TestValidateFileUnknown` — .txt extension defaults to JSON
4. `TestValidateFileNotFound` — non-existent file
5. `TestValidateDataInvalidYAML` — invalid YAML data
6. `TestValidateTypeNumber` — int, int64, float64
7. `TestValidateTypeObject` — map[string]interface{}
8. `TestValidateTypeUnknown` — unknown type returns true (no validation)

## Task 13: Improve internal/telemetry coverage (48.1% → 70%+)
**Source:** `internal/telemetry/telemetry.go` (167 lines)
**Existing tests:** `telemetry_test.go` (107 lines)
**Uncovered code:**
- HTTPExporter: ExportSpans, Flush, timeout default
- Service: NewService defaults (Timeout, BatchSize)
- generateID: counter increment

**New test cases:**
1. `TestHTTPExporterNew` — verify default timeout
2. `TestHTTPExporterFlushEmpty` — flush with no spans
3. `TestHTTPExporterExportSpans` — mock HTTP server, verify POST
4. `TestServiceDefaults` — NewService with zero values
5. `TestGenerateID` — verify counter increments
6. `TestSpanCount` — verify count after multiple EndSpan

## Task 14: Sync OpenAPI spec
**File:** `docs/openapi.yaml`
**Issues:**
- Version is `"0.8.0"` → should be `"1.0.0"`
- Missing endpoints: `/version`, `/config/schema`, `/pipelines`, `/.well-known/openid-configuration`, `/.well-known/jwks.json`, `/metrics`, `/healthz`, `/readyz`
- Missing DELETE `/artifacts` path

## Task 15: Update VERSION to 1.0.0
**File:** `VERSION` — change `0.9.0` to `1.0.0`

## Task 16: Update CHANGELOG for 1.0.0
**File:** `CHANGELOG.md` — add `## [1.0.0]` section with all changes made

## Execution Order
1. Task 1 (CI fix) — 1 min
2. Tasks 2-13 (test coverage) — bulk of work, ~12 tasks
3. Task 14 (OpenAPI sync) — 5 min
4. Task 15 (VERSION) — 1 min
5. Task 16 (CHANGELOG) — 5 min
6. Final verification: `go build ./... && go vet ./... && go test ./...`
