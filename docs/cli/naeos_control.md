## naeos control

Governance control plane evaluation

### Synopsis

Evaluate authorization requests against registered policies to produce deterministic decisions (ALLOW, DENY, REQUIRE_APPROVAL).

```
naeos control [flags]
```

### Options

```
  -h, --help   help for control
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos](naeos.md)	 - NAEOS CLI - Declarative Engineering Runtime
* [naeos control evaluate](naeos_control_evaluate.md)	 - Evaluate an authorization request and issue a decision

