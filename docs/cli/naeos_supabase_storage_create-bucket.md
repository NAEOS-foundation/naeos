## naeos supabase storage create-bucket

Create a new storage bucket

```
naeos supabase storage create-bucket [flags]
```

### Options

```
  -h, --help          help for create-bucket
      --name string   bucket name (required)
      --public        make bucket public
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos supabase storage](naeos_supabase_storage.md)	 - Supabase storage management

