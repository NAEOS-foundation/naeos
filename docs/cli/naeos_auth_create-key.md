## naeos auth create-key

Create a new API key

```
naeos auth create-key [flags]
```

### Options

```
  -h, --help             help for create-key
      --name string      key name (required)
      --user-id string   associated user ID (required)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos auth](naeos_auth.md)	 - Authentication and authorization management

