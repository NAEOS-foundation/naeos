## naeos broker publish

Publish a message to a channel

```
naeos broker publish [flags]
```

### Options

```
      --channel string   channel name (required)
  -h, --help             help for publish
      --message string   message payload (required)
      --name string      broker name (required)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos broker](naeos_broker.md)	 - Message broker management

