## naeos perf cache-set

Set a cache value

```
naeos perf cache-set [flags]
```

### Options

```
  -h, --help           help for cache-set
      --key string     cache key (required)
      --ttl duration   time to live (default 5m0s)
      --value string   cache value (required)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos perf](naeos_perf.md)	 - Performance optimization tools

