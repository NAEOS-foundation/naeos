## naeos perf pool-stats

Show connection pool statistics

```
naeos perf pool-stats [flags]
```

### Options

```
  -h, --help            help for pool-stats
      --name string     pool name (required)
  -o, --output string   output format: text, json, yaml
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos perf](naeos_perf.md)	 - Performance optimization tools

