## naeos gateway cb-status

Show circuit breaker status

```
naeos gateway cb-status [flags]
```

### Options

```
  -h, --help            help for cb-status
      --name string     circuit breaker name (default "default")
  -o, --output string   output format: text, json, yaml
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos gateway](naeos_gateway.md)	 - API gateway management

