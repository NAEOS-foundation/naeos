## naeos plugin execute

Execute a plugin action

```
naeos plugin execute [name] [action] [--params json] [flags]
```

### Options

```
  -h, --help            help for execute
      --params string   JSON parameters for the action
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --plugin-dir string      plugin directory (default "/home/codespace/.naeos/plugins")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos plugin](naeos_plugin.md)	 - Manage NAEOS plugins

