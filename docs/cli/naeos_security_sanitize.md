## naeos security sanitize

Sanitize input string

```
naeos security sanitize [flags]
```

### Options

```
  -h, --help           help for sanitize
      --input string   input to sanitize (required)
      --mode string    sanitize mode (html, sql, xss, path, all) (default "all")
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos security](naeos_security.md)	 - Security and secrets management

