## naeos supabase storage download

Download a file from storage

```
naeos supabase storage download [flags]
```

### Options

```
      --bucket string   bucket name (required)
      --dest string     local destination path (required)
  -h, --help            help for download
      --source string   remote file path (required)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos supabase storage](naeos_supabase_storage.md)	 - Supabase storage management

