## naeos marketplace plugin uninstall

Uninstall a plugin

```
naeos marketplace plugin uninstall [name] [flags]
```

### Options

```
  -h, --help   help for uninstall
```

### Options inherited from parent commands

```
      --cache-dir string       cache directory (default ".naeos/cache")
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --registry string        remote registry URL (default uses local cache)
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos marketplace plugin](naeos_marketplace_plugin.md)	 - Manage marketplace plugins

