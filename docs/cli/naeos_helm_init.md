## naeos helm init

Initialize a new Helm chart

```
naeos helm init <name> [flags]
```

### Options

```
      --app-version string   app version (default 1.0.0)
      --description string   chart description
  -h, --help                 help for init
  -o, --output string        output directory (default: <name>)
      --version string       chart version (default 0.1.0)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos helm](naeos_helm.md)	 - Helm chart scaffolding and validation

