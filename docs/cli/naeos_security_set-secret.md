## naeos security set-secret

Store an encrypted secret

```
naeos security set-secret [flags]
```

### Options

```
  -h, --help           help for set-secret
      --key string     encryption key (required)
      --name string    secret name (required)
      --value string   secret value (required)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos security](naeos_security.md)	 - Security and secrets management

