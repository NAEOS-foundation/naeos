## naeos workspace lock

Generate or update workspace lockfile

### Synopsis

Generate a naeos.lock file that pins all module versions for reproducible builds.

```
naeos workspace lock [flags]
```

### Options

```
  -h, --help   help for lock
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --root string            workspace root directory (default ".")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos workspace](naeos_workspace.md)	 - Manage multi-module workspaces

