## naeos serve uninstall

Remove the installed systemd unit

```
naeos serve uninstall [flags]
```

### Options

```
  -h, --help   help for uninstall
      --user   operate on the user systemd unit instead of the system unit
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos serve](naeos_serve.md)	 - Run NAEOS as a production daemon

