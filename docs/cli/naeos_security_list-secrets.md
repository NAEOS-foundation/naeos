## naeos security list-secrets

List all stored secrets

```
naeos security list-secrets [flags]
```

### Options

```
  -h, --help         help for list-secrets
      --key string   encryption key (optional; defaults to built-in key)
```

### Options inherited from parent commands

```
      --dry-run                global dry-run mode: preview without writing to disk
      --output-format string   output format: json, yaml, table (default "table")
      --verbose                enable verbose logging
```

### SEE ALSO

* [naeos security](naeos_security.md)	 - Security and secrets management

